<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.wontlost</groupId>
    <artifactId>ckeditor-vaadin</artifactId>
    <packaging>jar</packaging>
    <version>5.4.0</version>
    <name>CKEditorVaadin</name>
    <description>Integration of CKEditor 5 for Vaadin</description>
    <url>https://github.com/wontlost-ltd/vaadin-ckeditor</url>
    <scm>
        <url>https://github.com/wontlost-ltd/vaadin-ckeditor</url>
        <connection>scm:git:https://github.com/wontlost-ltd/vaadin-ckeditor.git</connection>
        <developerConnection>scm:git:git@github.com:wontlost-ltd/vaadin-ckeditor.git</developerConnection>
    </scm>
    <properties>
        <vaadin.version>25.2.6</vaadin.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>
    <organization>
        <name>WontLost Ltd</name>
    </organization>
    <licenses>
        <license>
            <name>Apache 2</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <developers>
        <developer>
            <name>WontLost Ltd</name>
            <email>service@wontlost.com</email>
            <organization>WontLost Ltd</organization>
            <organizationUrl>https://github.com/wontlost-ltd</organizationUrl>
        </developer>
    </developers>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-bom</artifactId>
                <type>pom</type>
                <scope>import</scope>
                <version>${vaadin.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Vaadin (provided - supplied by consuming application) -->
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-core</artifactId>
            <scope>provided</scope>
        </dependency>

        <!--
            Jackson JSON 3.x（provided - Vaadin 25+ 的 setPropertyJson 使用）。
            不显式声明 version：vaadin-bom 已透传 tools.jackson:jackson-bom
            （25.2.6 对应 3.1.5），由其统一管理版本。这样既跟随 Vaadin 的安全基线
            自动获得补丁版本，也避免我们写死的版本反过来把消费端拉回旧版
            （此前写死 3.1.4 即触发 CVE-2026-59889）。
        -->
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>3.1.5</version>
            <scope>provided</scope>
        </dependency>

        <!--
            HTML Sanitization。1.23.1 起修复 CVE-2026-71497：Cleaner 在处理
            自定义 raw-text 元素时可能放行本应被清理的标记，直接影响
            HtmlSanitizer 的 Jsoup.clean(html, Safelist) 路径，故必须保持在
            补丁版本之上（这是本库真正随包分发的运行时依赖）。
        -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.23.1</version>
        </dependency>

        <!-- Jakarta Servlet API (provided - Vaadin 25+) -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.1.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>6.0.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.7</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <index>true</index>
                        <manifest>
                            <addClasspath>false</addClasspath>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                        <manifestEntries>
                            <Vaadin-Package-Version>1</Vaadin-Package-Version>
                        </manifestEntries>
                        <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
                    </archive>
                    <excludes>
                        <exclude>META-INF/VAADIN/config/flow-build-info.json</exclude>
                        <exclude>**/node_modules/**</exclude>
                        <exclude>**/package-lock.json</exclude>
                        <exclude>**/tsconfig.json</exclude>
                        <!--
                            测试资产不应随发布产物分发：它们 import vitest（消费方不会安装的
                            devDependency），既是无谓体积（约占 jar 的 46%），也让消费端的
                            Vaadin 前端构建存在扫描到不可解析 import 的风险。
                        -->
                        <exclude>**/*.test.ts</exclude>
                        <exclude>**/test-mocks/**</exclude>
                        <exclude>**/vitest.config.ts</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>biz.aQute.bnd</groupId>
                <artifactId>bnd-maven-plugin</artifactId>
                <version>7.2.1</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>bnd-process</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.10.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <!--
                            source-plugin 有独立的 excludes，不继承 jar-plugin 的配置。
                            若不在此排除，本地构建（node_modules 存在于
                            src/main/resources/.../vaadin-ckeditor/ 下）产出的 sources jar
                            会包含约 2 万个 node_modules 文件、体积达 49MB。
                            CI 全新 checkout 无 node_modules 故已发布产物未受影响，
                            但本地 `mvn -Prelease deploy` 会把它推上 Central 且不可撤回，
                            同时造成第三方许可文件混入本项目 Apache-2 sources jar 的合规问题。
                        -->
                        <configuration>
                            <excludes>
                                <exclude>**/node_modules/**</exclude>
                                <exclude>**/*.test.ts</exclude>
                                <exclude>**/test-mocks/**</exclude>
                                <exclude>**/vitest.config.ts</exclude>
                            </excludes>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.10.1</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <quiet>true</quiet>
                            <doclint>none</doclint>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.8</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
